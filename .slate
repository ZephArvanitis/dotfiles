# bind key:modifiers operation parameter+

alias key alt

# Monitor handling
alias mon-laptop 0
alias mon-dell 1

# Restart slate after editing .slate
bind r:${key} relaunch

# Basic window rearrangement
bind a:${key} move screenOriginX;screenOriginY screenSizeX/2;screenSizeY
bind d:${key} move screenOriginX+screenSizeX/2;screenOriginY screenSizeX/2;screenSizeY
bind s:${key} move screenOriginX;screenOriginY+screenSizeY/2 screenSizeX;screenSizeY/2
bind w:${key} move screenOriginX;screenOriginY screenSizeX;screenSizeY/2
bind x:${key} move screenOriginX;screenOriginY screenSizeX;screenSizeY

# Throw window to other display
bind u:${key} throw next

# Hints!
bind h:${key} hint asdfghjkl

# Application shortcuts
#    a – taken for window rearrangement
#b
bind c:${key} focus "Chrome"
#    d – taken for window rearrangement
#e
bind f:${key} focus "Firefox"
bind g:${key} focus "Safari"
#h
#i
#j
bind k:${key} focus "Anki"
bind l:${key} focus "Calendar"
bind m:${key} focus "Thunderbird"
#n
bind o:${key} focus "WhatsApp"
bind p:${key} focus "Preview"
bind q:${key} focus "Slack"
#    r – taken for slate refresh
#    s – taken for window rearrangement
bind t:${key} focus "iTerm2"
#    u – taken for window rearrangement
bind v:${key} focus "Code" # VS Code, designated "Code" in activity monitor
#    w – taken for window rearrangement
#    x – taken for window rearrangement
bind y:${key} focus "TiddlyDesktop"
bind z:${key} focus "Azure Data Studio"
